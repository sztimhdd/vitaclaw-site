{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "npx serve@latest out",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
